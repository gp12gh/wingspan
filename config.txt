# Configuration file for Wingspan

# Where to find the source (MyML) files for the articles
folder_source = C:\bcnn\source

# Where to put the resulting HTML files (web server root)
folder_output = C:\bcnnweb

# Where to find the template file
filespec_template = C:\bcnn\source\template.txt

# Where to find the manifest
filespec_manifest = C:\bcnn\source\manifest.txt

# The name of the stylesheet file
filename_stylesheet = bcstyle.css

# The html <title> of all pages
title_text = BC Norfolk Newsletter

# First line of header block on all pages
page_headline = Norfolk Branch Newsletter

#PDF files are named as prefix{slug} e.g. bcnn108.pdf
pdf_prefix = bcnn

# Contact information in the page footer
contact_email = ed@bcnorfolk.uk
